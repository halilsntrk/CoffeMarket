@model CoffeeMarketPanel.Models.Response.UserLoginResponse
@{ 

}
<div class="container-fluid">
    <div class="row">
      
        <!-- Ana İçerik -->
        <main role="main" class="col-md-9 ml-sm-auto col-lg-10 px-md-4">
            <!-- İçerik Buraya Gelecek -->
            @if (true)
            {
                @Html.Partial("~/Views/Product/Add.cshtml")
            }
            else if (true)
            {
                @Html.Partial("~/Views/Product/Detail.cshtml")
            }

            <h2>Dashboard</h2>
            <p>Mevcut sayfa içeriği burada yer alacak.</p>
        </main>
    </div>
</div>